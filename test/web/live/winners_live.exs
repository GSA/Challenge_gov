defmodule Web.WinnersLiveTest do
  use Web.ConnCase

  describe "managing winners for a given challenge" do
    test "manage winners btn enabled only after phase is complete" do
      assert true == false
    end
  end
end
